import React, { FD } from 'react';
import { View, ViewProps } from 'react-native';

interface WrapperProps extends ViewProps {

}

const Wrapper: FD<WrapperProps> = () => {
	return <View />
}

export default Wrapper